# Raw audio is large and third-party (EGFxSet, or your own recordings) --
# rebuild it locally with organize_dataset.py rather than committing it.
# The .gitkeep files that preserve the folder structure are NOT ignored.
data/raw_audio/**/*.wav

# Generated feature datasets
data/features/*.csv

# Trained models and reports
models/
reports/

# Dataset archives and temporary downloads
*.zip
dataset_download/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# OS / editor cruft
.DS_Store
.vscode/

data/raw_egfxset/